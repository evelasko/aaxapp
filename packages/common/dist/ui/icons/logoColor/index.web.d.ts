import { Component } from 'react';
interface Props {
    width: number;
    height: number;
    color: string;
}
export default class LogoColor extends Component<Props> {
    render(): JSX.Element;
}
export {};
